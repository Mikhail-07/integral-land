---
description: "Правила разработки для INTEGRAL LANDING - технический стек, структура проекта, цветовая палитра, принципы написания кода"
globs:
  - "src/**/*.{js,jsx,ts,tsx}"
  - "src/components/**/*"
  - "src/styles/**/*"
  - "src/utils/**/*"
alwaysApply: true
---

# INTEGRAL LANDING - Правила разработки

## 📋 Описание проекта

**INTEGRAL LANDING** - лендинг для демонстрации контрольно-пропускного пункта (КПП) нового поколения.

### 🎯 Цель проекта

Создать современный, адаптивный лендинг, который демонстрирует функциональность и преимущества КПП через интерактивные блоки и анимации.

### 🏗️ Структура лендинга

1. **Hero** - главный блок с описанием и изображением КПП
2. **SplitView** - разрез КПП по пополам
3. **TopView** - анимация вида сверху
4. **Carousel** - карусель компонентов КПП
5. **Gallery** - галерея фотографий изнутри
6. **Environment** - анимация окружения КПП
7. **Specifications** - технические характеристики
8. **FAQ** - часто задаваемые вопросы
9. **ContactForm** - форма обратной связи
10. **Footer** - подвал сайта

## 🛠️ Технический стек

### Frontend

- **React 19** - основная библиотека
- **Vite** - сборщик и dev-сервер
- **Tailwind CSS 4** - CSS-фреймворк


### Дополнительные технологии

- **ESLint** - линтер кода
- **PostCSS** - обработка CSS

## 📁 Структура проекта

```
src/
├── components/           # React компоненты
│   ├── ui/              # Базовые UI компоненты
│   │   ├── Button.jsx
│   │   ├── Card.jsx
│   │   ├── Container.jsx
│   │   └── Section.jsx
│   ├── layout/          # Компоненты макета
│   │   ├── AppHeader.jsx
│   │   ├── Footer.jsx
│   │   └── Navigation.jsx
│   └── sections/        # Секции лендинга
│       ├── Hero/
│       ├── SplitView/
│       ├── TopView/
│       ├── Carousel/
│       ├── Gallery/
│       ├── Environment/
│       ├── Specifications/
│       ├── FAQ/
│       └── ContactForm/
├── hooks/               # Кастомные React хуки
│   ├── useIntersectionObserver.js
│   └── useCarousel.js
├── utils/               # Утилиты и константы
│   ├── colors.js        # Цветовая палитра
│   └── animations.js
├── assets/              # Статические ресурсы
│   └── images/
├── styles/              # CSS стили
│   └── components.css
├── App.jsx              # Главный компонент
├── main.jsx             # Точка входа
└── index.css            # Основные стили + Tailwind
```

## 🎨 Цветовая палитра

### Основные цвета бренда

```css
--color-brand-deep-teal: #0e403f    /* Основной фон */
--color-brand-white: #ffffff         /* Основной текст */
--color-brand-amber: #e1b425        /* Акцент */
```

### Цвета ссылок

```css
--color-link: #4c9dcb               /* Обычная ссылка */
--color-link-hover: #73b6e0         /* Ховер */
--color-link-active: #3679a4        /* Активная */
```

### UI цвета

```css
--color-ui-success: #5fb57b         /* Успех */
--color-ui-error: #c74343           /* Ошибка */
--color-ui-warning: #e29f10         /* Предупреждение */
```

### Оттенки серого

```css
--color-grey-1: #9ca6a4            /* Основной серый */
--color-grey-2: #f3f5f4            /* Светлый фон */
--color-grey-3: #f0f0f0            /* Очень светлый */
--color-grey-4: #dadada            /* Средний */
--color-grey-5: #d0d4d2            /* Средне-светлый */
--color-grey-6: #b8c1bf            /* Средне-темный */
--color-grey-7: #9ca6a4            /* Мутный текст */
--color-grey-8: #5f6f6e            /* Темно-серый */
```

### Разделители и панели

```css
--color-divider: #2c4a48            /* Границы */
--color-panel: #142c2b              /* Темные панели */
```

## 📝 Правила написания кода

### 🎯 Принципы разработки

1. **Максимальное дробление** - разбивать компоненты на логические части
2. **Переиспользование кода** - создавать универсальные компоненты
3. **Читаемость** - код должен быть понятным для других разработчиков
4. **Консистентность** - единый стиль во всем проекте
5. **Чистота кода** - простые, понятные и легко поддерживаемые решения
6. **Документирование** - описывать сложную логику и API компонентов

### 🧩 Компоненты

- Каждый компонент в отдельном файле
- Использовать функциональные компоненты с хуками
- Пропсы типизировать через JSDoc или TypeScript
- Компоненты должны быть чистыми и предсказуемыми

### 🧹 Чистота и понятность кода

#### **Названия и структура:**

- **Понятные названия** - переменные, функции и компоненты должны называться так, чтобы их назначение было очевидно
- **Английские названия** - использовать английский язык для названий функций, переменных и файлов
- **Консистентность** - придерживаться единого стиля именования во всем проекте
- **Длина названий** - избегать слишком коротких (a, b) и слишком длинных названий

#### **Структура функций:**

- **Одна функция - одна задача** - каждая функция должна выполнять только одну конкретную задачу
- **Максимум 20-30 строк** - если функция длиннее, разбить на подфункции
- **Понятная логика** - избегать сложных вложенных условий и циклов

#### **Комментарии и документация:**

- **JSDoc для компонентов** - описывать назначение, пропсы и примеры использования
- **Комментарии к сложной логике** - объяснять неочевидные решения
- **README для сложных компонентов** - создавать документацию для сложных секций

### 🎨 Стилизация

- **ТОЛЬКО Tailwind CSS классы** - никаких inline стилей
- Использовать кастомные цвета из палитры
- Создавать переиспользуемые CSS классы в `components.css`
- Адаптивность для всех устройств

### 📱 Адаптивность

- Mobile-first подход
- Брейкпоинты: `sm:`, `md:`, `lg:`, `xl:`, `2xl:`
- Тестировать на разных разрешениях

### 🚀 Производительность

- Ленивая загрузка изображений
- Оптимизация анимаций
- Минимизация ре-рендеров
- Использование `React.memo` где необходимо

## 🔧 Настройка окружения

### Установка зависимостей

```bash
npm install
```

### Запуск dev-сервера

```bash
yarn dev
```

### Сборка проекта

```bash
yarn build
```

### Линтинг

```bash
npm run lint
```

## 📚 Примеры использования

### Создание компонента

```jsx
// src/components/ui/Button.jsx
export default function Button({ children, variant = "primary", ...props }) {
  const baseClasses =
    "px-6 py-3 rounded-lg font-semibold transition-all duration-200"
  const variants = {
    primary: "bg-brand-amber text-brand-deep-teal hover:bg-brand-amber/90",
    secondary: "bg-panel text-brand-white hover:bg-panel/90",
    outline:
      "border-2 border-brand-amber text-brand-amber hover:bg-brand-amber hover:text-brand-deep-teal",
  }

  return (
    <button className={`${baseClasses} ${variants[variant]}`} {...props}>
      {children}
    </button>
  )
}
```

### Использование цветов

```jsx
import { COLORS } from "../utils/colors"

// В компоненте
;<div className="bg-brand-deep-teal text-brand-white">
  <span className="text-brand-amber">Акцентный текст</span>
</div>
```

## 🚫 Что НЕ делать

- ❌ Не использовать inline стили
- ❌ Не создавать компоненты больше 100 строк
- ❌ Не дублировать код
- ❌ Не забывать про адаптивность
- ❌ Не использовать цвета вне палитры
- ❌ Не создавать сложные CSS анимации без необходимости
- ❌ Не писать функции длиннее 30 строк
- ❌ Не использовать непонятные сокращения в названиях
- ❌ Не создавать сложную вложенную логику
- ❌ Не забывать документировать сложные решения

## ✅ Что делать

- ✅ Создавать переиспользуемые компоненты
- ✅ Использовать семантическую HTML разметку
- ✅ Писать понятные названия переменных и функций
- ✅ Добавлять комментарии к сложной логике
- ✅ Тестировать на разных устройствах
- ✅ Следовать принципам доступности (a11y)
- ✅ Писать чистый и понятный код
- ✅ Документировать API компонентов через JSDoc
- ✅ Разбивать сложные функции на простые части
- ✅ Использовать английский язык для названий

## 🔄 Workflow

1. **Создание компонента** → `src/components/...`
2. **Стилизация** → Tailwind CSS классы
3. **Тестирование** → проверка на разных разрешениях
4. **Рефакторинг** → оптимизация и переиспользование
5. **Коммит** → понятное описание изменений

---

**Последнее обновление:** 03.09.2025
**Версия:** 1.0.0
